"use client";

/**
 * Highlights is now embedded directly in the Hero component
 * (angled banner + project cards), matching the reference site's
 * single-wrapper architecture. This file is kept as a re-export stub.
 */
export function Highlights() {
    return null;
}
